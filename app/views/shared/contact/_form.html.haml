= form_for contact, url: url, remote: remote do |f|
  = f.invisible_captcha :subject
  = f.label :name
  %small#name_hint= t("helpers.placeholder.contact.name")
  = f.text_field :name, aria: { describedby: "name_hint" }, label: false
  = f.text_field :email
  = f.text_area :message, rows: 7
  = f.submit t(".submit"), class: "large button expand success"
